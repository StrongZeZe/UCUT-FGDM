(1) manus_deal_allCode.py is an example of the manus_deal_allcode.py implementation

(2) The run_sensitive_datas.pu file implements sensitivity analysis

(3) The Run_simulation_datas.py file outputs the simulation data analysis results

(4) Because there are many external variables involved, the amount of calculation is slightly large, so lindo14 cannot output the loop, so the important functions are placed in the folder in the form of external files.

The data folder contains the main data in.pkl format